[project]
name = "hermes-telegram-downloader"
version = "1.0.0"
description = "Telegram 媒体下载 / 转发 / 监听工具，带任务持久化 + 崩溃恢复 + 现代 WebUI"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.14"
dependencies = [
    # pyrogram 补丁 fork（上游不可用，保持直连）
    "pyrogram @ https://github.com/tangyoha/pyrogram/archive/refs/heads/patch.zip",
    "pyyaml>=6.0.2",
    "rich>=13.0",
    "pytgcrypto>=1.2.7",
    "loguru>=0.7",
    "werkzeug>=3.0",
    "flask>=3.0",
    "ply>=3.11",
    "ruamel.yaml>=0.18",
    "flask-login>=0.6.3",
    "pycryptodome>=3.20",
    "requests>=2.32",
]

[project.scripts]
media-downloader = "hermes_telegram_downloader.media_downloader:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hermes_telegram_downloader"]

[tool.hatch.metadata]
allow-direct-references = true

[dependency-groups]
dev = [
    "ruff>=0.8",
    "pytest>=8.0",
]

[tool.ruff]
target-version = "py314"
src = ["src"]

[tool.ruff.lint]
select = ["F"]
ignore = ["F401", "F841", "F811", "F541"]

[tool.ruff.lint.per-file-ignores]
"src/hermes_telegram_downloader/media_downloader.py" = ["F821"]

[tool.pytest.ini_options]
testpaths = ["tests"]
